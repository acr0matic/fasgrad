const path = '../src';

const modalParams = {
  awaitCloseAnimation: true,
  disableScroll: true,
  disableFocus: false,
};

const scrollParams = {
  speed: 500,
  speedAsDuration: true,
  offset: 0,
}